import React from 'react';
import Link from 'next/link';

interface AuthProps {
}

function Auth({
  ...props
}: AuthProps) {
  
  return (
    <React.Fragment>
        AUTHENTICATION :-)
    </React.Fragment>
  );
};

export default Auth;
